--Challenge: Book list database
--What are your favorite books? You can make a database table to store them in! In this first step, create a table to store your list of books. It should have columns for id, name, and rating. Add three of your favorite books into the table.
--Solution:

CREATE TABLE books (
    id INTEGER PRIMARY KEY,
    name TEXT,
    rating INTEGER);

INSERT INTO books 
    VALUES (1,'HP',5),(2,'John',4),(3,'Spiders',2);
SELECT * FROM books;
SELECT * FROM books;
SELECT * FROM books;
